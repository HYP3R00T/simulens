[project]
name = "simulens-workspace"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "simulens",
]

[tool.uv.workspace]
members = [
  "packages/*",
]

[tool.uv.sources]
simulens = { workspace = true }
