[project]
name = "simple-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "simulens",
]

[tool.uv.sources]
simulens = { workspace = true }
